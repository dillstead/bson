#include "bson.h"

struct bson_res bson_parse(struct arena *arena, struct str str,
                           struct bson_obj **obj)
{
    struct bson_res res = { BRC_BADSTATE, 0};

    return res;
}
